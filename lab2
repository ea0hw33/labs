% Сформировать новый список из всех четных элементов исходного списка
even_elements([], []). % Базовый случай: если исходный список пуст, то и результирующий список также пуст
even_elements([Head|Tail], [Head|Result]) :-
    0 is Head mod 2, % Проверяем, является ли текущий элемент четным
    even_elements(Tail, Result). % Рекурсивно вызываем эту же функцию для хвоста списка
even_elements([_|Tail], Result) :-
    even_elements(Tail, Result). % Если текущий элемент нечетный, просто пропускаем его и продолжаем обработку


% even_elements([1, 2, 3, 4, 5, 6], Result).
% Result = [2, 4, 6] .
